'use client'

// Dashboard page now shows chat as main content (handled in layout)
// This component is kept for potential future dashboard widgets
export default function DashboardPage() {
  return null
}
