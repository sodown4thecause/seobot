# Product Requirement Document (PRD): GEO & SEO Content Engine Mode

## 1. Executive Summary & Core Vision
This document details the product requirements for the new **GEO (Generative Engine Optimization)** and **SEO Content Engine** modes. Built on Next.js, Vercel AI SDK, and Tailwind CSS 4, this feature elevates standard AI chat interactions into a rich, visual workbench. The core user experience centers on high-fidelity, real-time client-side streaming of structured **Artifacts** (interactive dashboards, keyword tables, and complete blog content layouts) that automatically adapt to the user's brand identity and design themes.

## 2. System Architecture & Data Pipeline
To ensure high deliverability, avoid CAPTCHAs, and control operational expenditures, the system splits background scraping tasks from client-side execution using a hybrid architecture of MCP-integrated extraction (DataForSEO, Jina, Firecrawl) and self-managed Vultr VPS instances running the geomode/Elmo fork.

### 2.1 GEO Scraping Infrastructure Specification

| Component | Infrastructure Role | Implementation / Setup Details |
| :--- | :--- | :--- |
| **Geomode/Elmo + MCP Extraction** | Primary Extraction Gateway | geomode/Elmo fork on a Vultr VPS captures raw engine data from Google AI Overviews, ChatGPT, Claude, Perplexity, and Gemini. DataForSEO SERP API, Jina reader, and Firecrawl (via `@ai-sdk/mcp`) handle structured extraction and rendering without a third-party scraping SaaS dependency. |
| **Vultr VPS Nodes** | Proxy Rotation & Cron Worker Cluster | Lightweight Ubuntu instances acting as custom egress proxies or queue workers. These workers orchestrate scheduled interval tracking (daily/weekly tracking cron jobs) to feed baseline market intelligence into the database without bottlenecking the main application server. |
| **Next.js Backend (Route Handlers)** | API Orchestrator | Secures API tokens, structures queries based on user intent classifier prompts, normalizes varying JSON payloads from the scraping nodes, and maps them directly into structured tool calls via the Vercel AI SDK. |

**Scraping Frequency & Trigger Logic:**
- **On-Demand (Event-Driven):** Triggered instantly when a user inputs a specific tracking prompt or initializes a live competitive audit inside the chat stream.
- **Scheduled Tracking (Cron):** Batched daily or weekly across target keywords to map visual trend data without exceeding DataForSEO/Jina rate limits.

## 3. Core Artifact Matrix (10 Visual Components)
To provide a standardized yet comprehensive diagnostic experience, the interface renders exactly 10 key Artifacts inside the client-side streaming workspace. These components use pure client-side state management and Tailwind CSS 4 utility classes for theme adaptation.

| # | Artifact Name | Functional Description & Dynamic Metrics Displayed |
| :-: | :--- | :--- |
| 1 | **GEO Share of Voice Dashboard** | A comparative visual breakdown showing the user's presence percentage vs competitors across the 5 target networks (Google AIO, ChatGPT, Claude, Perplexity, Gemini). |
| 2 | **AI Source Citation Tracker** | A dedicated grid mapping exactly which URLs are being referenced by the Generative Engines for specific thematic keyword phrases, displaying citation frequency counts. |
| 3 | **Keyword Position Volatility Table** | A tabular UI reporting traditional SEO keyword positions tracked against their net impact within Generative responses (e.g., Cited vs. Omitted status). |
| 4 | **Competitor Content Delta Map** | An analytical breakdown pointing out content gaps—highlighting semantic keywords and topics covered by competing domains that are currently missing from the user's site. |
| 5 | **Sentiment & Brand Context Panel** | An AI-driven sentiment tracker displaying whether the brand context generated by LLMs is Neutral, Positive, or Negative, along with extracted descriptive adjectives. |
| 6 | **Blog Content Hero Layout** | A full-width structured Content Mode preview featuring a curated design theme, tailored H1 titles, subheadings, and programmatic placeholders for rich visual blocks. |
| 7 | **Dynamic Theme Thumbnail & Image Component** | A dedicated visual preview component displaying asset parameters (dimensions, alternative text, thematic tags) for custom generated theme graphics (e.g., "Interior Design" mood boards). |
| 8 | **Optimization Recommendation Checklist** | An actionable, interactive client-side checklist prioritizing copy modifications required to push the content into LLM training and retrieval layers. |
| 9 | **Semantic Term Enrichment Table** | A structured keyword component detailing recommended LSI (Latent Semantic Indexing) terms, density ratios, and co-occurrence values required for GEO visibility. |
| 10 | **Live Response Delta Streamer** | A side-by-side terminal interface showing changes over time in raw outputs from Perplexity, ChatGPT, Gemini, and Claude for specified keyword prompts. |

## 4. Streaming & Client-Side React Rendering
To preserve high execution performance and interactivity, the application routes all JSON-streamed data directly into pure client-side components using the Vercel AI SDK.

### 4.1 Component Ingestion & State Architecture
- **Vercel AI SDK Integration:** Utilize `experimental_maxDuration` handles alongside the `streamObject` or `streamUI` utilities to yield valid, incremental JSON streams directly into the client context.
- **Client-Side State Tracking:** All incoming data chunks are accumulated and verified on the client via React state hooks. Components read incoming structural markers to display elegant skeletal loading wrappers until JSON parsing is complete.
- **Tailwind CSS 4 Dynamic Themes:** Components pull CSS variables or inline attributes driven by user configuration files to automatically apply appropriate brand styling (e.g., `style={{ '--brand-color': theme.primary }}` combined with Tailwind utility configurations).

## 5. Content Mode Integration & Visual Assets Flow
When a user requests a creative action via Content Mode (e.g., inputting "interior design blog"), the LLM orchestrator executes a multi-step dynamic generation flow inside the single chat stream:
1. **Intent Classification:** The system identifies the broad visual and structural theme from the user's brief input.
2. **Asset Schema Initialization:** The system injects structural slots for a Blog Header, a high-impact Hero Image asset framework, an optimized Thumbnail, and body components down the stream pipeline.
3. **Thematic Token Ingestion:** Layout components match Tailwind CSS 4 styles directly to the parsed topic—applying appropriate mood layouts, clean structural padding, and modern typographic scaling entirely on the client side.