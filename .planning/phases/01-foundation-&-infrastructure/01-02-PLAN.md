---
phase: 01-foundation-&-infrastructure
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - lib/jobs/inngest-client.ts
  - lib/jobs/functions/refresh-dashboard.ts
  - lib/jobs/middleware/cost-tracking.ts
  - app/api/inngest/route.ts
  - vercel.json
  - package.json
autonomous: true
user_setup:
  - service: inngest
    why: "Job orchestration for background data refresh"
    env_vars:
      - name: INNGEST_SIGNING_KEY
        source: "Inngest Dashboard -> Signing Keys"
      - name: INNGEST_EVENT_KEY
        source: "Inngest Dashboard -> Event Keys"
    dashboard_config:
      - task: "Create new app in Inngest dashboard"
        location: "Inngest Dashboard -> Apps -> Create App"
      - task: "Configure webhook endpoint"
        location: "Inngest Dashboard -> Endpoints -> Add https://your-domain.com/api/inngest"

must_haves:
  truths:
    - "Background jobs can be triggered via Inngest"
    - "Jobs have retry logic with exponential backoff"
    - "Job status is tracked in database (refreshJobs table)"
    - "API costs are tracked per job execution"
    - "Circuit breaker pattern prevents cascade failures"
    - "Jobs support cancellation mid-execution"
  artifacts:
    - path: "lib/jobs/inngest-client.ts"
      provides: "Inngest client configuration with middleware"
    - path: "lib/jobs/functions/refresh-dashboard.ts"
      provides: "Dashboard refresh job function with circuit breaker"
    - path: "lib/jobs/middleware/cost-tracking.ts"
      provides: "Cost tracking middleware for DataForSEO calls"
    - path: "app/api/inngest/route.ts"
      provides: "API route handler for Inngest webhooks"
    - path: "lib/jobs/circuit-breaker.ts"
      provides: "Circuit breaker implementation using opossum"
  key_links:
    - from: "refresh-dashboard job"
      to: "lib/db/schema refreshJobs table"
      via: "drizzle ORM updates"
    - from: "cost-tracking middleware"
      to: "lib/db/schema apiUsageEvents table"
      via: "drizzle insert"
    - from: "circuit-breaker"
      to: "DataForSEO MCP tools"
      via: "wrapped calls with timeout/fallback"
---

<objective>
Implement Inngest job orchestration for dashboard data refresh with circuit breaker pattern, cost tracking, and retry logic.

Purpose: Dashboard data refresh requires reliable background job processing that handles DataForSEO API calls without blocking the user. Inngest provides durable execution, retries, and real-time status updates. Circuit breaker prevents cascade failures when DataForSEO is unavailable.
Output: Working Inngest setup with dashboard refresh job, cost tracking middleware, and circuit breaker protection.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@lib/db/schema.ts
@lib/mcp/dataforseo-client.ts
@01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Inngest and opossum dependencies</name>
  <files>package.json</files>
  <action>
Install required dependencies for job orchestration:

```bash
npm install inngest opossum
npm install --save-dev @types/opossum
```

IMPORTANT: Do NOT add inngest to devDependencies - it's a runtime dependency. Use exact version inngest@^3.0.0 as specified in research.

After install, verify package.json shows:
- "inngest": "^3.0.0"
- "opossum": "^8.x"
  </action>
  <verify>npm list inngest opossum && grep -E '"inngest"|"opossum"' package.json</verify>
  <done>inngest and opossum packages installed with correct versions</done>
</task>

<task type="auto">
  <name>Task 2: Create Inngest client with middleware</name>
  <files>lib/jobs/inngest-client.ts</files>
  <action>
Create Inngest client configuration at lib/jobs/inngest-client.ts:

1. Import { Inngest } from 'inngest'
2. Import cost tracking middleware from ./middleware/cost-tracking
3. Create client with:
   - id: 'seobot-dashboard'
   - name: 'SEOBOT Dashboard Jobs'
   - middleware: [costTrackingMiddleware]
   - eventKey from process.env.INNGEST_EVENT_KEY

4. Export inngest client as singleton
5. Define event types:
   - 'dashboard/refresh.requested'
   - 'dashboard/refresh.progress'
   - 'dashboard/refresh.completed'
   - 'dashboard/refresh.failed'

6. Create type-safe event payload interfaces using zod

IMPORTANT: Follow the existing pattern in lib/mcp/ where clients are singleton exports. Do NOT create multiple Inngest instances. Use environment validation with validateEnv pattern from scripts/validate-env.ts.
  </action>
  <verify>npx tsc --noEmit && node -e "require('./lib/jobs/inngest-client.ts')" 2>&1 | head -5</verify>
  <done>Inngest client created with middleware and type-safe events</done>
</task>

<task type="auto">
  <name>Task 3: Implement cost tracking middleware</name>
  <files>lib/jobs/middleware/cost-tracking.ts</files>
  <action>
Create cost tracking middleware at lib/jobs/middleware/cost-tracking.ts:

1. Create middleware function that wraps DataForSEO calls
2. Track per-call costs by:
   - Recording endpoint name
   - Recording method (standard vs live)
   - Calculating cost based on DataForSEO pricing
   - Storing in apiUsageEvents table via Drizzle

3. Cost calculation logic:
   - SERP Live: $0.005 per request
   - SERP Standard: $0.001 per task
   - Backlinks: $0.01 per request
   - Other: $0.001 per request

4. Middleware should:
   - Accept job context (userId, jobId)
   - Wrap the actual API call
   - Log cost regardless of success/failure
   - Handle async task pattern (POST -> poll -> GET)

5. Export middleware function for use in job functions

CRITICAL: Must insert into apiUsageEvents table (created in 01-01) with proper Drizzle ORM calls.
  </action>
  <verify>npx tsc --noEmit && grep -l "apiUsageEvents" lib/jobs/middleware/cost-tracking.ts</verify>
  <done>Cost tracking middleware implemented with DataForSEO pricing</done>
</task>

</tasks>

<verification>
1. Dependencies installed: `npm list inngest opossum`
2. TypeScript compiles: `npx tsc --noEmit`
3. Inngest client exports correctly: `node -e "const { inngest } = require('./lib/jobs/inngest-client.ts'); console.log('OK')"`
4. Middleware imports correctly: `grep -l "cost-tracking" lib/jobs/inngest-client.ts`
</verification>

<success_criteria>
- Inngest client configured with proper event types
- Cost tracking middleware implemented and wired into client
- Middleware correctly calculates and logs DataForSEO costs
- TypeScript compilation passes for all new files
- Ready for job function implementation (next plan)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-&-infrastructure/01-02-SUMMARY.md` with:
- Inngest client configuration
- Middleware architecture
- Cost calculation formulas
- Dependencies for job functions
</output>
