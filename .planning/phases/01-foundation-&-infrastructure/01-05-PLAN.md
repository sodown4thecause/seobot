---
phase: 01-foundation-&-infrastructure
plan: 05
type: execute
wave: 3
depends_on:
  - 01-02
  - 01-04
files_modified:
  - components/dashboard/sidebar.tsx
  - components/dashboard/breadcrumbs.tsx
  - components/dashboard/freshness-indicator.tsx
  - components/dashboard/refresh-button.tsx
  - components/dashboard/job-progress.tsx
  - lib/hooks/use-job-status.ts
  - app/api/jobs/sse/route.ts
  - app/dashboard/layout.tsx
  - lib/hooks/use-freshness.ts
autonomous: false
user_setup: []

must_haves:
  truths:
    - "Sidebar shows 8 dashboard links with icons and labels"
    - "Active dashboard highlighted in sidebar"
    - "Breadcrumbs show current location (Home > Dashboard > [Name])"
    - "Every widget displays 'Last updated: X hours ago'"
    - "Freshness color-coded: <24h green, 24-48h yellow, >48h red"
    - "Global 'Refresh Now' button in header triggers background job"
    - "Job progress visible via SSE with real-time updates"
    - "Skeleton loaders shown while data loads (not spinners)"
    - "Partial data displays while background refresh completes"
  artifacts:
    - path: "components/dashboard/sidebar.tsx"
      provides: "8-dashboard navigation with active state"
    - path: "components/dashboard/breadcrumbs.tsx"
      provides: "Location breadcrumbs with home/root links"
    - path: "components/dashboard/freshness-indicator.tsx"
      provides: "Color-coded freshness badge with timestamp"
    - path: "components/dashboard/refresh-button.tsx"
      provides: "Global refresh trigger with loading state"
    - path: "components/dashboard/job-progress.tsx"
      provides: "Progress bar with status and cancel option"
    - path: "lib/hooks/use-job-status.ts"
      provides: "SSE hook for real-time job updates"
    - path: "app/api/jobs/sse/route.ts"
      provides: "Server-Sent Events endpoint for job status"
  key_links:
    - from: "use-job-status hook"
      to: "app/api/jobs/sse"
      via: "EventSource connection"
    - from: "SSE endpoint"
      to: "lib/db/schema refreshJobs"
      via: "poll job status from database"
    - from: "refresh-button"
      to: "lib/jobs/inngest-client"
      via: "send dashboard/refresh.requested event"
---

<objective>
Build dashboard UX components: sidebar navigation, freshness indicators, refresh controls, and real-time job status via SSE.

Purpose: Users need visual navigation to 8 dashboards, clear indication of data freshness, and ability to trigger refreshes. Real-time job status shows progress and builds trust. Skeleton loaders and partial data display prevent jarring loading states.
Output: Complete dashboard UX layer with navigation, freshness UI, and real-time job status updates.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@lib/db/schema.ts
@lib/jobs/inngest-client.ts
@components/ui/ (existing shadcn components)
@01-02-SUMMARY.md
@01-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create sidebar navigation with 8 dashboards</name>
  <files>components/dashboard/sidebar.tsx
components/dashboard/breadcrumbs.tsx</files>
  <action>
Create dashboard navigation components:

1. Create components/dashboard/sidebar.tsx:
   - Use shadcn/ui components (already available in components/ui/)
   - Import: Collapsible, ScrollArea, Button from @radix-ui
   - 8 dashboard links with icons:
     - Overview (LayoutDashboard icon)
     - Website Audit (Search icon)
     - Rank Tracker (TrendingUp icon)
     - Competitor Monitor (Users icon)
     - Keyword Opportunities (Key icon)
     - Backlink Profile (Link icon)
     - Content Performance (FileText icon)
     - AEO Insights (Sparkles icon)
   - Active state highlighting (use pathname from next/navigation)
   - Collapsible for mobile (use useState for isOpen)
   - Sticky positioning with proper z-index

2. Create components/dashboard/breadcrumbs.tsx:
   - Accept current page name prop
   - Breadcrumb structure: Home > Dashboard > [CurrentPage]
   - Use shadcn/ui breadcrumb components
   - Home link to /dashboard
   - Dashboard link to /dashboard/overview (default)

3. Both components should:
   - Use existing Tailwind classes from project
   - Follow shadcn/ui patterns (cn() utility, cva where needed)
   - Be TypeScript with proper props interfaces
   - Support dark mode (use existing dark: classes)

CRITICAL: Use EXISTING shadcn/ui components from components/ui/. Do NOT create new UI primitives - import from existing.
  </action>
  <verify>npx tsc --noEmit && ls -la components/dashboard/sidebar.tsx components/dashboard/breadcrumbs.tsx</verify>
  <done>Sidebar with 8 dashboards and breadcrumbs created using shadcn/ui</done>
</task>

<task type="auto">
  <name>Task 2: Create freshness indicator and refresh button</name>
  <files>components/dashboard/freshness-indicator.tsx
components/dashboard/refresh-button.tsx
lib/hooks/use-freshness.ts</files>
  <action>
Create freshness UI components:

1. Create lib/hooks/use-freshness.ts:
   - Calculate freshness from lastUpdated timestamp
   - Return: { freshness: 'fresh'|'stale'|'expired', hoursAgo: number, color: string }
   - Logic:
     - < 24 hours = fresh (green-500)
     - 24-48 hours = stale (yellow-500)
     - > 48 hours = expired (red-500)

2. Create components/dashboard/freshness-indicator.tsx:
   - Accept lastUpdated: Date prop
   - Use useFreshness hook
   - Display: "Last updated: X hours ago" with color-coded badge
   - Badge colors: green for fresh, yellow for stale, red for expired
   - Small text size (text-xs or text-sm)
   - Show tooltip on hover with exact timestamp

3. Create components/dashboard/refresh-button.tsx:
   - Global refresh button for header placement
   - States: idle, loading (with spinner), complete
   - On click: send Inngest event via lib/jobs/inngest-client
   - Show disabled state when refresh in progress
   - Success toast on completion (use sonner if available, or alert)
   - Optional: Show estimated cost before refresh

4. Export both from components/dashboard/index.ts

IMPORTANT: Refresh button must trigger dashboard/refresh.requested event to Inngest (from 01-02). Handle auth via Clerk useAuth().
  </action>
  <verify>npx tsc --noEmit && grep -E "freshness|lastUpdated" components/dashboard/freshness-indicator.tsx</verify>
  <done>Freshness indicator with color coding and refresh button implemented</done>
</task>

<task type="auto">
  <name>Task 3: Implement SSE for real-time job status</name>
  <files>app/api/jobs/sse/route.ts
lib/hooks/use-job-status.ts
components/dashboard/job-progress.tsx</files>
  <action>
Implement Server-Sent Events for job status updates:

1. Create app/api/jobs/sse/route.ts:
   - Route handler using export async function GET
   - Set headers for SSE: Content-Type: text/event-stream
   - Connection ID generation (nanoid or uuid)
   - Poll refreshJobs table every 2 seconds via Drizzle
   - Send events: { type: 'progress', jobId, progress, status }
   - Handle client disconnect (cleanup interval)
   - Rate limit connections (per user, max 1 connection)
   - Auth via Clerk: getAuth(req) and validate userId

2. Create lib/hooks/use-job-status.ts:
   - useEffect to create EventSource connection
   - Connect to /api/jobs/sse
   - Parse incoming events
   - Return: { job, progress, status, error, isConnected }
   - Auto-reconnect on disconnect (max 5 retries)
   - Cleanup on unmount

3. Create components/dashboard/job-progress.tsx:
   - Display progress bar (0-100%)
   - Show status text: "Queued...", "Processing SERP data...", "Complete"
   - Cancel button (sends cancellation request)
   - Auto-hide on completion (after 5 seconds)
   - Use Progress component from shadcn/ui
   - Position: Fixed or inline in header

CRITICAL: SSE is one-way (server to client), NOT WebSockets. Use native EventSource API (not Socket.io). Handle auth via query param or cookie.
  </action>
  <verify>npx tsc --noEmit && grep -l "EventSource\|text/event-stream" app/api/jobs/sse/route.ts lib/hooks/use-job-status.ts</verify>
  <done>SSE endpoint and React hook for real-time job progress implemented</done>
</task>

</tasks>

<verification>
1. TypeScript compiles: `npx tsc --noEmit`
2. Components exist: `ls components/dashboard/*.tsx | wc -l` (should be >= 5)
3. Hooks exist: `ls lib/hooks/*.ts | grep -E "use-freshness|use-job-status"`
4. SSE route exists: `ls app/api/jobs/sse/route.ts`
5. All imports valid: Check for @/ paths resolving correctly
</verification>

<success_criteria>
- Sidebar component with 8 dashboard links and active state
- Breadcrumbs component showing Home > Dashboard > [Page]
- Freshness indicator showing "X hours ago" with color coding
- Refresh button triggering Inngest events
- SSE endpoint at /api/jobs/sse with proper auth
- useJobStatus hook connecting to SSE
- JobProgress component with progress bar and cancel
- All components use existing shadcn/ui primitives
- TypeScript compilation passes
- Dashboard layout integrated with navigation
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-&-infrastructure/01-05-SUMMARY.md` with:
- Component inventory
- SSE architecture
- Freshness calculation logic
- Checkpoint for visual verification
</output>
