---
phase: 01-foundation-&-infrastructure
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - lib/dataforseo/rate-limiter.ts
  - lib/dataforseo/circuit-breaker.ts
  - lib/dataforseo/client.ts
  - lib/dataforseo/types.ts
  - lib/constants/dataforseo-limits.ts
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Rate limits enforced per DataForSEO endpoint (not global)"
    - "General endpoints: 2000/min, Live Google Ads: 12/min, User Data: 6/min, Tasks Ready: 20/min"
    - "Circuit breaker opens after 5 consecutive failures"
    - "Circuit breaker has 30s timeout before half-open state"
    - "Standard method used for scheduled updates, Live only for user-triggered"
    - "Bulk polling via tasks_ready endpoint for cost optimization"
    - "Async task pattern handled (POST -> poll -> GET results)"
  artifacts:
    - path: "lib/dataforseo/rate-limiter.ts"
      provides: "Per-endpoint rate limiting with Upstash Redis"
    - path: "lib/dataforseo/circuit-breaker.ts"
      provides: "Circuit breaker using opossum"
    - path: "lib/dataforseo/client.ts"
      provides: "Wrapped DataForSEO client with rate limiting and circuit breaker"
    - path: "lib/constants/dataforseo-limits.ts"
      provides: "Rate limit constants per endpoint type"
  key_links:
    - from: "rate-limiter"
      to: "@upstash/redis"
      via: "Redis-based token bucket"
    - from: "circuit-breaker"
      to: "DataForSEO MCP tools"
      via: "wrapped execution with fallback"
    - from: "client.ts"
      to: "lib/db/schema apiUsageEvents"
      via: "cost tracking integration"
---

<objective>
Build DataForSEO integration layer with per-endpoint rate limiting, circuit breaker pattern, and cost-optimized request handling.

Purpose: DataForSEO has strict per-endpoint rate limits that vary by API type. Without proper rate limiting, we hit 429 errors and lose data. Circuit breaker prevents cascade failures when the API is degraded. Bulk polling via tasks_ready saves 60-80% on costs.
Output: Production-ready DataForSEO client with rate limiting, circuit breaker, and cost optimization.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@lib/db/schema.ts
@lib/mcp/dataforseo-client.ts
@lib/mcp/dataforseo/*.ts
@package.json
@01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create rate limit constants and endpoint mapping</name>
  <files>lib/constants/dataforseo-limits.ts</files>
  <action>
Create lib/constants/dataforseo-limits.ts with per-endpoint rate limits:

1. Define rate limit tiers:
   - GENERAL: 2000 requests/min (most endpoints)
   - LIVE_GOOGLE_ADS: 12 requests/min
   - USER_DATA: 6 requests/min  
   - TASKS_READY: 20 requests/min
   - DATABASE_MAX_SIMULTANEOUS: 30 requests

2. Create endpoint-to-tier mapping:
   - serp_organic_live -> GENERAL
   - serp_google_ads_live -> LIVE_GOOGLE_ADS
   - business_data -> USER_DATA
   - keywords_data -> USER_DATA
   - tasks_ready -> TASKS_READY
   - etc.

3. Export constants:
   - RATE_LIMITS (Record<LimitTier, number>)
   - ENDPOINT_TIERS (Record<string, LimitTier>)
   - getRateLimitForEndpoint(endpoint: string): number

4. Add method cost constants:
   - METHOD_STANDARD: 0.001 (per task, bulk)
   - METHOD_LIVE: 0.005 (per request, instant)

Follow existing pattern in lib/constants/ if it exists, otherwise create the directory.
  </action>
  <verify>grep -E "RATE_LIMITS|ENDPOINT_TIERS|METHOD_" lib/constants/dataforseo-limits.ts && npx tsc --noEmit</verify>
  <done>Rate limit constants defined with endpoint mapping and cost tiers</done>
</task>

<task type="auto">
  <name>Task 2: Implement Redis-based per-endpoint rate limiter</name>
  <files>lib/dataforseo/rate-limiter.ts</files>
  <action>
Create lib/dataforseo/rate-limiter.ts with token bucket rate limiting:

1. Import @upstash/redis client (already in package.json)
2. Import rate limit constants from lib/constants/dataforseo-limits
3. Create RateLimiter class:
   - Key: `rate_limit:{endpoint}:{minute_bucket}`
   - Token bucket algorithm with Redis INCR + EXPIRE
   - Check before each request
   - Throw RateLimitExceededError if exceeded

4. Implement methods:
   - checkLimit(endpoint: string): Promise<boolean>
   - consumeToken(endpoint: string): Promise<void>
   - getRemainingTokens(endpoint: string): Promise<number>

5. Handle time window bucketing (per-minute windows aligned to clock)

6. Export singleton rateLimiter instance

CRITICAL: Must use Upstash Redis (HTTP-based, serverless-compatible) not node-redis. Follow existing @upstash/redis usage patterns if present.

7. Add retry-after calculation for 429 responses
  </action>
  <verify>npx tsc --noEmit && grep -l "@upstash/redis" lib/dataforseo/rate-limiter.ts</verify>
  <done>Token bucket rate limiter implemented with per-endpoint limits</done>
</task>

<task type="auto">
  <name>Task 3: Implement circuit breaker pattern</name>
  <files>lib/dataforseo/circuit-breaker.ts</files>
  <action>
Create lib/dataforseo/circuit-breaker.ts using opossum:

1. Import CircuitBreaker from 'opossum' (installed in 01-02)
2. Import existing DataForSEO MCP client from lib/mcp/dataforseo-client.ts
3. Create circuit breaker configuration:
   - timeout: 10s (fail fast, don't wait for DataForSEO timeouts)
   - errorThresholdPercentage: 50 (open after 50% failure rate)
   - resetTimeout: 30000 (30s before half-open)
   - volumeThreshold: 5 (minimum calls before checking)

4. Implement fallback behavior:
   - Return cached data if available
   - Return empty result with warning flag
   - Log circuit open events

5. Create wrapper function:
   - callWithBreaker<T>(endpoint: string, fn: () => Promise<T>): Promise<T>
   - Tracks per-endpoint circuit state
   - Emits events: open, halfOpen, close, fallback

6. Export circuit breaker states for monitoring

7. Integrate with rate limiter (check rate limit before attempting call)

CRITICAL: The fallback MUST check dashboardData table for cached results before returning empty.
  </action>
  <verify>npx tsc --noEmit && grep -l "opossum\|CircuitBreaker" lib/dataforseo/circuit-breaker.ts</verify>
  <done>Circuit breaker implemented with 10s timeout and fallback to cache</done>
</task>

</tasks>

<verification>
1. TypeScript compiles: `npx tsc --noEmit`
2. Rate limiter exports: `node -e "const rl = require('./lib/dataforseo/rate-limiter.ts'); console.log('exports:', Object.keys(rl))"`
3. Circuit breaker exports: `node -e "const cb = require('./lib/dataforseo/circuit-breaker.ts'); console.log('exports:', Object.keys(cb))"`
4. Constants exported: `grep -E "export.*RATE_LIMITS|export.*ENDPOINT_TIERS" lib/constants/dataforseo-limits.ts`
</verification>

<success_criteria>
- Per-endpoint rate limits defined (GENERAL: 2000/min, LIVE_GOOGLE_ADS: 12/min, etc.)
- Redis-based token bucket rate limiter working
- Circuit breaker with 10s timeout and 30s reset
- Fallback to cached data when circuit is open
- TypeScript compilation passes
- Ready for integration with job functions
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-&-infrastructure/01-03-SUMMARY.md` with:
- Rate limiting strategy by endpoint type
- Circuit breaker configuration
- Fallback behavior specification
- Integration points for job functions
</output>
